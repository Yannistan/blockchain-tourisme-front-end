/* import React, { useContext, useState } from "react";

import { TourismeContext } from "../App";
import {TourTokenContext} from "../App";
import { Web3Context } from "../hooks/useWeb3";

const PayReservation = () => {

const { web3State } = useContext(Web3Context);
const TourToken = useContext(TourTokenContext); 
const [priceTokens, setPriceTokens] = useState(0);
const [ID, setID] = useState(0);
// const nbTokens = ethers.utils.parseEther(priceTokens);
 
const handlePayReservation = async () => {
    await TourToken.reserveByClient(ID);
}

};

*/
